---
title: Python Programmierung
icon: 🐍
---

# Python Programmierung

Variablen sind wie Boxen, in denen man Daten speichern kann. Man gibt ihnen einen Namen und weist ihnen mit `=` einen Wert zu.

```python
name = "Max"
alter = 15
ist_bereit = True
```

## Datentypen

Es gibt verschiedene Arten von Daten:
- **String (Text):** `"Hallo"`
- **Integer (Ganzzahl):** `42`
- **Float (Kommazahl):** `3.14`
- **Boolean (Wahrheitswert):** `True` oder `False`

## Funktionen

Funktionen sind Code-Blöcke, die eine bestimmte Aufgabe erledigen. Sie starten mit `def` und können Werte mit `return` zurückgeben.

```python
def verdopple(n):
    return n * 2

print(verdopple(10)) # Gibt 20 aus
```

## Schleifen (Loops)

Mit einer for-Schleife kannst du Code mehrmals ausführen.

```python
for i in range(5):
    print("Hallo") # Wird 5x gedruckt
```
